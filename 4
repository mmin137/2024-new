#include <stdio.h>

int main(void) {
  int num, result[50]={}, i=0;
  printf("10진수 입력 : ");
  scanf("%d", &num);

  while(num>0) { 
    result[i] = num%2;
    num = num/2;
    i++;
    }

  for(int j=i-1; j>=0; j--) {
    printf("%d", result[j]);
  }
}
